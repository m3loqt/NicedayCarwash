# Copy to functions/.env for local emulator use. Not used in production deploys for secrets -
# MAYA_PUBLIC_KEY and MAYA_SECRET_KEY are Firebase secrets (see README below), never plain env vars.

# "production" switches both createCheckout and the webhook to Maya's live hosts.
# Leave unset (or anything else) to use Maya's sandbox hosts.
MAYA_ENVIRONMENT=
